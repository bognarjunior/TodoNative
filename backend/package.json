{
  "name": "backend",
  "version": "1.0.0",
  "description": "Servidor de simulação usando json-server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Bognar Junior",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.14.0"
  }
}
